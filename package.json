{
  "name": "limousine-booking-api",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server db.json --host 0.0.0.0 --port $PORT --cors",
    "dev": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}